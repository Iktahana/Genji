interface:
  display_name: "Safe Commit & Push"
  short_description: "Review, commit, and safely push intended changes"
  default_prompt: "Use $safe-commit-push to review my intended changes, create suitable commits, and push them safely."
